# {{PROJECT_NAME}}

Generated with `setup-node-api`.

## Scripts

- `npm run dev` starts the API in watch mode
- `npm start` starts the API

## Endpoints

- `GET /health` returns the API health status
- `POST /echo` sends back the JSON body you post
{{README_CORS_NOTE}}
